# 1. What are Pseudo-Elements?

### **Definition**

A **pseudo-element** lets you style **specific parts** of an element’s content that aren’t actual elements in the HTML DOM.

It’s written with **double colons (`::`)**.

**Syntax:**

```css
selector::pseudo-element {
    property: value;
}
```

---

### **Common Examples**

| Pseudo-Element   | What it does                                | Example                                |
| ---------------- | ------------------------------------------- | -------------------------------------- |
| `::before`       | Inserts content before an element’s content | `p::before { content: "→ "; }`         |
| `::after`        | Inserts content after an element’s content  | `p::after { content: " ✓"; }`          |
| `::first-letter` | Styles the first letter of text             | `p::first-letter { font-size: 2rem; }` |
| `::first-line`   | Styles the first line of text               | `p::first-line { font-weight: bold; }` |
| `::selection`    | Styles the text selection color             | `::selection { background: yellow; }`  |
| `::placeholder`  | Styles placeholder text in inputs           | `input::placeholder { color: gray; }`  |

---

### 🧠 Think of it like:

> “Pseudo-elements create _virtual elements_ that don’t exist in HTML, but can be styled.”

Example:

```css
p::after {
    content: " (read more)";
    color: gray;
}
```

This will _virtually_ add “(read more)” after every `<p>` element’s text.

---

### 🪶 Tailwind Equivalent

Tailwind supports pseudo-elements using **variants** and **content utilities**:

```html
<p class="before:content-['→'] after:content-['✓']">Hello</p>
```

This is equivalent to:

```css
p::before {
    content: "→";
}
p::after {
    content: "✓";
}
```

---

### Difference Between Pseudo-Classes and Pseudo-Elements

| Feature             | Pseudo-Class                       | Pseudo-Element                            |
| ------------------- | ---------------------------------- | ----------------------------------------- |
| **Prefix**          | `:` (single colon)                 | `::` (double colon)                       |
| **Purpose**         | Represents a state                 | Represents part of an element             |
| **Affects**         | Whole element                      | Part of the element (text, content, etc.) |
| **Example**         | `:hover`, `:focus`, `:nth-child()` | `::before`, `::after`, `::first-letter`   |
| **Tailwind Syntax** | `hover:bg-blue-500`                | `before:content-['']`                     |

---

# 2. Pseudo Element

They always start with **double colons `::`**

```css
selector::pseudo-element {
    property: value;
}
```

## 🧩 1. **::before**

Creates a virtual element **before** the content of the selected element.

Used often for adding icons, symbols, or decorative content.

```css
p::before {
    content: "👉 ";
    color: blue;
}
```

🪶 **Tailwind Equivalent:**

```html
<p class="before:content-['👉'] before:text-blue-500">Hello</p>
```

---

## 🧩 2. **::after**

Creates a virtual element **after** the content of the selected element.

Commonly used for tooltips, arrows, or decorative elements.

```css
p::after {
    content: " ✅";
    color: green;
}
```

🪶 **Tailwind Equivalent:**

```html
<p class="after:content-['✅'] after:text-green-500">Task done</p>
```

---

## 🧩 3. **::first-letter**

Selects and styles the **first letter** of text in a block-level element.

```css
p::first-letter {
    font-size: 2rem;
    font-weight: bold;
    color: crimson;
}
```

🪶 **Tailwind Equivalent:**
Not built-in directly — but can be done with `first-letter:` variant.

```html
<p class="first-letter:text-crimson first-letter:text-2xl first-letter:font-bold">Welcome to CSS!</p>
```

---

## 🧩 4. **::first-line**

Targets and styles the **first line** of text within a block element.

```css
p::first-line {
    font-weight: bold;
    color: navy;
}
```

🪶 **Tailwind Equivalent:**

```html
<p class="first-line:font-bold first-line:text-blue-700">This is the first line only.</p>
```

---

## 🧩 5. **::selection**

Applies styles to the portion of text that is **selected** by the user.

```css
::selection {
    background: yellow;
    color: black;
}
```

🪶 **Tailwind Equivalent:**

```html
<p class="selection:bg-yellow-300 selection:text-black">Select this text to see effect.</p>
```

---

## 🧩 6. **::placeholder**

Styles the **placeholder text** inside `<input>` or `<textarea>` elements.

```css
input::placeholder {
    color: gray;
    font-style: italic;
}
```

🪶 **Tailwind Equivalent:**

```html
<input class="placeholder:text-gray-400 placeholder:italic" placeholder="Enter your name" />
```

---

## 🧩 7. **::marker**

Styles the **bullet or numbering marker** of a list item.

```css
li::marker {
    color: red;
    font-size: 1.5em;
}
```

🪶 **Tailwind Equivalent:**

```html
<ul class="list-disc marker:text-red-500 marker:text-xl">
    <li>Important</li>
</ul>
```

---

## 🧩 8. **::file-selector-button**

Targets the **button** part of a file input (`<input type="file">`).

```css
input::file-selector-button {
    background-color: blue;
    color: white;
    border-radius: 5px;
}
```

🪶 **Tailwind Equivalent:**

```html
<input type="file" class="file:bg-blue-500 file:text-white file:rounded-md" />
```

---

## 🧩 9. **::backdrop**

Used to style the **background layer** behind elements such as `<dialog>` or fullscreen elements.

```css
dialog::backdrop {
    background-color: rgba(0, 0, 0, 0.5);
}
```

🪶 **Tailwind Equivalent:**

```html
<dialog class="backdrop:bg-black/50">Hello!</dialog>
```

---

## 🧩 10. **::cue**

Styles WebVTT (subtitle) cues in media (used in videos with subtitles).

```css
::cue {
    color: white;
    background: rgba(0, 0, 0, 0.7);
}
```

🪶 No Tailwind equivalent (very niche).

---

## 🧩 11. **::part()**

Used with [Web Components](https://developer.mozilla.org/en-US/docs/Web/Web_Components) — styles elements marked with a `part` attribute inside shadow DOM.

```css
custom-element::part(button) {
    background: teal;
}
```

🪶 No Tailwind equivalent (depends on custom elements).

---

## 🧩 12. **::slotted()**

Used to style elements projected into a shadow DOM via `<slot>`.

```css
::slotted(span) {
    color: purple;
}
```

🪶 No Tailwind equivalent — specific to Shadow DOM.

---

## 🧩 13. **::cue-region**

Styles the **region** containing subtitles (WebVTT cues).

```css
::cue-region {
    background: rgba(0, 0, 0, 0.4);
}
```

🪶 No Tailwind equivalent — used for media captions.

---

## 🧩 14. **::spelling-error** and **::grammar-error**

Highlight browser-detected spelling or grammar mistakes.

```css
::spelling-error {
    text-decoration: red wavy underline;
}
::grammar-error {
    text-decoration: green wavy underline;
}
```

🪶 No Tailwind equivalent (browser handles this automatically).

---

## ✅ Summary Table

| Pseudo-Element           | Description                           | Example                                                     | Tailwind Support   |
| ------------------------ | ------------------------------------- | ----------------------------------------------------------- | ------------------ |
| `::before`               | Inserts content before element        | `p::before { content: "→ "; }`                              | ✅ `before:`       |
| `::after`                | Inserts content after element         | `p::after { content: "✓"; }`                                | ✅ `after:`        |
| `::first-letter`         | Styles first letter                   | `p::first-letter { font-size: 2rem; }`                      | ✅ `first-letter:` |
| `::first-line`           | Styles first line                     | `p::first-line { color: blue; }`                            | ✅ `first-line:`   |
| `::selection`            | Styles selected text                  | `::selection { background: yellow; }`                       | ✅ `selection:`    |
| `::placeholder`          | Styles placeholder text               | `input::placeholder { color: gray; }`                       | ✅ `placeholder:`  |
| `::marker`               | Styles list markers                   | `li::marker { color: red; }`                                | ✅ `marker:`       |
| `::file-selector-button` | Styles file upload button             | `input::file-selector-button { background: blue; }`         | ✅ `file:`         |
| `::backdrop`             | Styles modal or fullscreen background | `dialog::backdrop { background: rgba(0,0,0,.5); }`          | ✅ `backdrop:`     |
| `::cue`                  | Styles subtitles (WebVTT)             | `::cue { color: white; }`                                   | ❌                 |
| `::part()`               | Styles shadow DOM parts               | `::part(button)`                                            | ❌                 |
| `::slotted()`            | Styles slotted elements in shadow DOM | `::slotted(span)`                                           | ❌                 |
| `::cue-region`           | Styles subtitle region                | `::cue-region { background: black; }`                       | ❌                 |
| `::spelling-error`       | Styles misspelled words               | `::spelling-error { text-decoration: wavy red underline; }` | ❌                 |

---

## ⚙️ Summary Difference Reminder

| Concept            | Syntax | Targets                                                     |
| ------------------ | ------ | ----------------------------------------------------------- |
| **Pseudo-Class**   | `:`    | A _state_ (hovered, focused, checked)                       |
| **Pseudo-Element** | `::`   | A _part_ or _virtual portion_ (before, after, first-letter) |
