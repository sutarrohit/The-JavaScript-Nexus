# 🧩 **Tailwind CSS Flexbox Utilities**

Flexbox in Tailwind helps you build responsive, flexible layouts easily using utility classes.
It controls how elements align, grow, shrink, and wrap inside a container.

## 🔹 1. `flex-direction`

**Controls the direction in which flex items are placed inside a flex container.**
This sets the `flex-direction` CSS property.

### 🔸 Classes & CSS:

| Class              | CSS                               | Description                                  |
| ------------------ | --------------------------------- | -------------------------------------------- |
| `flex-row`         | `flex-direction: row;`            | Items placed **horizontally (left → right)** |
| `flex-row-reverse` | `flex-direction: row-reverse;`    | Items placed **horizontally (right → left)** |
| `flex-col`         | `flex-direction: column;`         | Items placed **vertically (top → bottom)**   |
| `flex-col-reverse` | `flex-direction: column-reverse;` | Items placed **vertically (bottom → top)**   |

### 🔸 Example:

```html
<div class="flex flex-col gap-4">
    <div class="bg-blue-300">01</div>
    <div class="bg-green-300">02</div>
    <div class="bg-yellow-300">03</div>
</div>
```

✅ `flex-col` → stacks items vertically

---

## 🔹 2. `flex-wrap`

**Controls whether flex items stay on one line or wrap onto multiple lines.**

### 🔸 Classes & CSS:

| Class               | CSS                        | Description                                      |
| ------------------- | -------------------------- | ------------------------------------------------ |
| `flex-nowrap`       | `flex-wrap: nowrap;`       | All items stay in one line                       |
| `flex-wrap`         | `flex-wrap: wrap;`         | Items wrap to next line when space runs out      |
| `flex-wrap-reverse` | `flex-wrap: wrap-reverse;` | Items wrap onto multiple lines, in reverse order |

### 🔸 Example:

```html
<div class="flex flex-wrap gap-2">
    <div class="w-32 h-16 bg-blue-300">01</div>
    <div class="w-32 h-16 bg-green-300">02</div>
    <div class="w-32 h-16 bg-yellow-300">03</div>
    <div class="w-32 h-16 bg-pink-300">04</div>
</div>
```

✅ Items wrap to next line when the container width is too small.

---

## 🔹 3. `flex-basis`

**Defines the initial size of a flex item before space distribution (grow/shrink).**

### 🔸 Classes & CSS:

| Class                | CSS                            | Description                   |
| -------------------- | ------------------------------ | ----------------------------- |
| `basis-0`            | `flex-basis: 0px;`             | Item starts at 0 width        |
| `basis-auto`         | `flex-basis: auto;`            | Item size is based on content |
| `basis-1/2`          | `flex-basis: 50%;`             | Item takes 50% of container   |
| `basis-[200px]`      | `flex-basis: 200px;`           | Custom basis value            |
| `basis-(--my-basis)` | `flex-basis: var(--my-basis);` | Uses CSS variable             |

### 🔸 Example:

```html
<div class="flex gap-2">
    <div class="basis-1/4 bg-blue-300">25%</div>
    <div class="basis-1/2 bg-green-300">50%</div>
    <div class="basis-1/4 bg-yellow-300">25%</div>
</div>
```

✅ Each box gets a base width relative to container width.

---

## 🔹 4. `flex-grow`

**Controls how much a flex item can grow relative to others when there’s extra space.**

### 🔸 Classes & CSS:

| Class              | CSS                          | Description                        |
| ------------------ | ---------------------------- | ---------------------------------- |
| `grow`             | `flex-grow: 1;`              | Item grows to fill available space |
| `grow-0`           | `flex-grow: 0;`              | Item doesn’t grow                  |
| `grow-[2]`         | `flex-grow: 2;`              | Grows twice as fast as others      |
| `grow-(--my-grow)` | `flex-grow: var(--my-grow);` | Uses CSS variable                  |

### 🔸 Example:

```html
<div class="flex gap-2">
    <div class="grow bg-blue-300">grow</div>
    <div class="grow-0 bg-green-300 w-32">no grow</div>
    <div class="grow-[2] bg-yellow-300">grow 2x</div>
</div>
```

✅ The `grow-[2]` item takes **twice as much space** as the others.

---

## 🔹 5. `flex-shrink`

**Controls how a flex item shrinks when there isn’t enough space.**

### 🔸 Classes & CSS:

| Class                  | CSS                              | Description                |
| ---------------------- | -------------------------------- | -------------------------- |
| `shrink`               | `flex-shrink: 1;`                | Item can shrink            |
| `shrink-0`             | `flex-shrink: 0;`                | Item cannot shrink         |
| `shrink-[2]`           | `flex-shrink: 2;`                | Shrinks faster than others |
| `shrink-(--my-shrink)` | `flex-shrink: var(--my-shrink);` | Uses CSS variable          |

### 🔸 Example:

```html
<div class="flex gap-2">
    <div class="w-64 shrink bg-blue-300">Shrink</div>
    <div class="w-64 shrink-0 bg-green-300">No shrink</div>
</div>
```

✅ When the container shrinks, the first box reduces its width, the second stays fixed.

---

## 🔹 6. `order`

**Controls the order in which flex items appear.**

By default, all flex items have `order: 0`.
You can change their order using positive or negative values.

### 🔸 Classes & CSS:

| Class         | CSS             | Description                    |
| ------------- | --------------- | ------------------------------ |
| `order-1`     | `order: 1;`     | Moves item to later position   |
| `order-2`     | `order: 2;`     | Moves item after order-1 items |
| `order-first` | `order: -9999;` | Moves item to very start       |
| `order-last`  | `order: 9999;`  | Moves item to very end         |
| `order-none`  | `order: 0;`     | Default order                  |

### 🔸 Example:

```html
<div class="flex gap-2">
    <div class="order-3 bg-blue-300">01</div>
    <div class="order-1 bg-green-300">02</div>
    <div class="order-2 bg-yellow-300">03</div>
</div>
```

✅ The items will appear in the order: **02 → 03 → 01**

---

## 🔹 7. `flex` Shorthand Utilities

Tailwind provides shorthand utilities that set `flex-grow`, `flex-shrink`, and `flex-basis` together.

| Class            | CSS Equivalent              | Description                       |
| ---------------- | --------------------------- | --------------------------------- |
| `flex-1`         | `flex: 1 1 0%;`             | Grow and shrink equally, basis 0% |
| `flex-auto`      | `flex: 1 1 auto;`           | Grow and shrink based on content  |
| `flex-initial`   | `flex: 0 1 auto;`           | Only shrink, don’t grow           |
| `flex-none`      | `flex: none;` → `0 0 auto;` | Fixed size, no grow/shrink        |
| `flex-[<value>]` | `flex: <custom>`            | Fully custom flex value           |
| `flex-(<var>)`   | `flex: var(--var)`          | Uses CSS variable                 |

### 🔸 Example:

```html
<div class="flex gap-4">
    <div class="flex-1 bg-blue-300">flex-1</div>
    <div class="flex-auto bg-green-300">flex-auto</div>
    <div class="flex-none w-32 bg-yellow-300">flex-none</div>
</div>
```

✅ `flex-1` expands equally
✅ `flex-auto` respects content width but can grow/shrink
✅ `flex-none` stays fixed

---

## 🔹 8. Combining Properties Example

```html
<div class="flex flex-wrap flex-row gap-2">
    <div class="basis-1/3 grow shrink bg-blue-300">Box 1</div>
    <div class="basis-1/3 grow-0 shrink-0 bg-green-300">Box 2</div>
    <div class="basis-1/3 grow shrink bg-yellow-300">Box 3</div>
</div>
```

✅ `Box 1` and `Box 3` expand and shrink
✅ `Box 2` stays fixed width
✅ Wrapping is allowed if the row becomes too tight

---

## 🔹 9. Responsive Usage

Tailwind makes all of these responsive using breakpoint prefixes:

```html
<div class="flex-col md:flex-row lg:flex-wrap">
    <div class="basis-1/2 grow md:order-last bg-blue-300">Responsive Box</div>
</div>
```

✅ On mobile: stacked vertically
✅ On medium screens: horizontal
✅ On large screens: wrapping
✅ Changes order on medium+

---

## ✅ Summary Table

| Concept            | Controls            | Key Classes                                        |
| ------------------ | ------------------- | -------------------------------------------------- |
| **Direction**      | Layout axis         | `flex-row`, `flex-col`, `flex-row-reverse`         |
| **Wrap**           | Line breaking       | `flex-wrap`, `flex-nowrap`                         |
| **Basis**          | Initial size        | `basis-0`, `basis-auto`, `basis-[200px]`           |
| **Grow**           | Expansion           | `grow`, `grow-0`, `grow-[2]`                       |
| **Shrink**         | Compression         | `shrink`, `shrink-0`, `shrink-[2]`                 |
| **Order**          | Position in DOM     | `order-1`, `order-first`, `order-last`             |
| **Flex shorthand** | Combined properties | `flex-1`, `flex-auto`, `flex-initial`, `flex-none` |
