## 🧩 Overview

| Method         | Purpose                                                                     | Mutates array? | Typical Use                            |
| -------------- | --------------------------------------------------------------------------- | -------------- | -------------------------------------- |
| `copyWithin()` | Copies a portion of the array **to another position** within the same array | ✅ Yes         | Fast internal data rearrangement       |
| `fill()`       | Fills all or part of the array with a **static value**                      | ✅ Yes         | Quickly initialize or overwrite values |

---

## 🔹 1. `copyWithin(target, start, end)`

### 🧠 Purpose:

Copies part of the array **to another position** in the same array **without changing its length**.

It’s like doing an **in-place copy** of a slice of the array.

### 🧩 Syntax:

```js
arr.copyWithin(target, start, end);
```

| Parameter | Description                                       |
| --------- | ------------------------------------------------- |
| `target`  | Index where the copied data will be written       |
| `start`   | Start index of data to copy (default = 0)         |
| `end`     | End index (non-inclusive, default = array.length) |

---

### 🧪 Example 1 – Basic usage

```js
let arr = [10, 20, 30, 40, 50];
arr.copyWithin(1, 3);

console.log(arr);
```

**Step-by-step:**

1. `start = 3` → take elements from index `3` onwards → `[40, 50]`
2. `target = 1` → start pasting from index `1`
3. Overwrite values starting at index 1

**Result:**

```
[10, 40, 50, 40, 50]
```

✅ The length stays the same (5 elements).
✅ Elements `20` and `30` are replaced by `40` and `50`.

---

### 🧪 Example 2 – With start and end

```js
let arr = [1, 2, 3, 4, 5, 6];
arr.copyWithin(0, 3, 5);

console.log(arr);
```

**Step-by-step:**

-   Copy elements from index `3` to `5` (non-inclusive) → `[4, 5]`
-   Paste them starting at index `0`

**Result:**

```
[4, 5, 3, 4, 5, 6]
```

---

### ⚙️ Notes:

-   Does **not** add or remove elements — only overwrites.
-   Works **in-place** for high performance (no new array).
-   Useful for:

    -   Shifting array segments.
    -   Duplicating patterns inside arrays.
    -   Low-level data manipulation in typed arrays.

---

## 🔹 2. `fill(value, start, end)`

### 🧠 Purpose:

Fills all or part of an array with a **static value**.

This is often used to **initialize** or **reset** arrays.

### 🧩 Syntax:

```js
arr.fill(value, start, end);
```

| Parameter | Description                                                   |
| --------- | ------------------------------------------------------------- |
| `value`   | Value to fill with                                            |
| `start`   | Index to begin filling (default = 0)                          |
| `end`     | Index to stop filling (non-inclusive, default = array.length) |

---

### 🧪 Example 1 – Fill entire array

```js
let arr = [1, 2, 3, 4];
arr.fill(0);

console.log(arr);
```

**Result:**

```
[0, 0, 0, 0]
```

✅ Every element replaced with `0`.

---

### 🧪 Example 2 – Fill partially

```js
let arr = [1, 2, 3, 4, 5];
arr.fill(9, 1, 4);

console.log(arr);
```

**Step-by-step:**

-   `value = 9`
-   `start = 1` → begin at index 1
-   `end = 4` → stop before index 4

**Result:**

```
[1, 9, 9, 9, 5]
```

✅ Only indices 1, 2, and 3 are replaced.

---

### 🧪 Example 3 – Initialize new arrays

You can combine `fill()` with the array constructor to **create pre-filled arrays**.

```js
const arr = new Array(5).fill("🍎");
console.log(arr);
```

**Result:**

```
["🍎", "🍎", "🍎", "🍎", "🍎"]
```

✅ Useful for placeholders, test data, or initializing grids.

---

### ⚙️ Notes:

-   Mutates the array.
-   Returns the same array reference (so it can be chained).
-   Common in:

    -   Initializing arrays
    -   Resetting arrays
    -   Building test data
    -   Grid-based logic (e.g., game boards)

---

## 🧠 Comparison Table

| Method                           | Purpose                                | Example                       | Output        | Mutates? |
| -------------------------------- | -------------------------------------- | ----------------------------- | ------------- | -------- |
| `copyWithin(target, start, end)` | Copy part of array to another position | `[1,2,3,4,5].copyWithin(1,3)` | `[1,4,5,4,5]` | ✅       |
| `fill(value, start, end)`        | Fill array (all or part) with a value  | `[1,2,3,4,5].fill(0,1,4)`     | `[1,0,0,0,5]` | ✅       |

---

## ⚡ Summary

-   **`copyWithin()`** → copies internal elements around inside the same array.
    _Think of it like “cut and paste within itself.”_
-   **`fill()`** → overwrites values with a single repeated one.
    _Think of it like “painting” the array with one color._

Both are **mutating**, **fast**, and useful for **low-level array manipulation**.
